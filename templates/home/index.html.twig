{% extends 'base.html.twig' %}

{% block title %}La Boutique Française - Accueil{% endblock %}

{% block body %}
    <!-- Hero Section -->
    <section class="hero-section section text-white" style="background-image: linear-gradient(rgba(0,0,0,0.35), rgba(0,0,0,0.2)), url('/images/hero-atelier.jpg'); background-size: cover; background-position: center; padding: 6rem 0;">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-7">
                    <h1 class="display-4 fw-bold mb-3">Découvrez l'Excellence Française</h1>
                    <p class="lead mb-4 text-white-50">Des sacs et bijoux d'exception, fabriqués en France avec passion et savoir-faire. Chaque pièce raconte une histoire, celle de nos artisans talentueux.</p>
                    <div class="d-flex gap-3">
                        <a href="{{ path('app_products') }}" class="btn btn-primary btn-lg" aria-label="Découvrir la boutique"><i class="fas fa-shopping-bag me-2"></i>Découvrir</a>
                        <a href="{{ path('app_about') }}" class="btn btn-outline-primary btn-lg" aria-label="En savoir plus sur la boutique"><i class="fas fa-info-circle me-2"></i>En savoir plus</a>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Featured Products -->
    <section class="py-5 section">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center mb-5">
                    <h2 class="section-title">Produits Mis en Avant</h2>
                    <p class="section-subtitle">Nos coups de cœur, sélectionnés avec soin</p>
                </div>
            </div>
            <div class="row">
                {% if featured_products|length > 0 %}
                    {% for product in featured_products %}
                        <div class="col-lg-3 col-md-6 mb-4">
                            {{ include('product/_card.html.twig', {'product': product}) }}
                        </div>
                    {% endfor %}
                {% else %}
                    {% for i in 1..4 %}
                        <div class="col-lg-3 col-md-6 mb-4">
                            <div class="card h-100 text-center p-4">
                                <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height:200px;">
                                    <i class="fas fa-shopping-bag text-muted" style="font-size:3rem;"></i>
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title">Bientôt disponible</h5>
                                    <p class="card-text text-muted">Découvrez nos nouveautés bientôt mises en ligne.</p>
                                    <a href="{{ path('app_products') }}" class="btn btn-outline-primary">Voir la boutique</a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% endif %}
            </div>
        </div>
    </section>

    <!-- Categories -->
    <section class="py-5 section">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center mb-5">
                    <h2 class="section-title">Nos Catégories</h2>
                    <p class="section-subtitle">Explorez notre gamme complète</p>
                </div>
            </div>
            <div class="row">
                {% if categories|length > 0 %}
                    {% for category in categories %}
                        <div class="col-lg-4 col-md-6 mb-4">
                            {% set cardClass = 'card text-center h-100' %}
                            {% if category.name == 'Bijoux' %}
                                {% set cardClass = 'card bijoux-card text-center h-100' %}
                            {% elseif category.name == 'Sacs' %}
                                {% set cardClass = 'card sacs-card text-center h-100' %}
                            {% endif %}
                            <div class="{{ cardClass }}">
                                {% if category.imageUrl %}
                                    <div class="card-img-top" style="height: 250px; overflow: hidden; display: flex; align-items: center; justify-content: center;">
                                        <img src="{{ category.imageUrl }}" alt="{{ category.name }}" style="width: 100%; height: 100%; object-fit: cover;">
                                    </div>
                                {% else %}
                                    <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height: 250px;">
                                        <i class="fas fa-tags text-muted" style="font-size: 3rem;"></i>
                                    </div>
                                {% endif %}
                                <div class="card-body">
                                    <h5 class="card-title">{{ category.name }}</h5>
                                    {% if category.description %}
                                        <p class="card-text text-muted">{{ category.description }}</p>
                                    {% endif %}
                                    <a href="{{ path('app_products_by_category', {'id': category.id}) }}" class="btn btn-primary" aria-label="Découvrir la catégorie {{ category.name }}">
                                        <i class="fas fa-arrow-right me-1"></i>Découvrir
                                    </a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% else %}
                    {% for i in 1..3 %}
                        <div class="col-lg-4 col-md-6 mb-4">
                            <div class="card text-center h-100 p-4">
                                <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height:200px;">
                                    <i class="fas fa-tags text-muted" style="font-size:3rem;"></i>
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title">Catégorie à venir</h5>
                                    <p class="card-text text-muted">Nos catégories seront bientôt disponibles. Inscrivez-vous pour être notifié.</p>
                                    <a href="{{ path('app_contact') }}" class="btn btn-outline-primary">Contactez-nous</a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% endif %}
            </div>
        </div>
    </section>

    <!-- Latest Products -->
    <section class="py-5 section">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center mb-5">
                    <h2 class="section-title">Nouveautés</h2>
                    <p class="section-subtitle">Nos dernières créations</p>
                </div>
            </div>
            <div class="row">
                {% if latest_products|length > 0 %}
                    {% for product in latest_products %}
                        <div class="col-lg-3 col-md-6 mb-4">
                            {{ include('product/_card.html.twig', {'product': product}) }}
                        </div>
                    {% endfor %}
                {% else %}
                    {% for i in 1..4 %}
                        <div class="col-lg-3 col-md-6 mb-4">
                            <div class="card h-100 text-center p-4">
                                <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height:200px;">
                                    <i class="fas fa-star text-muted" style="font-size:3rem;"></i>
                                </div>
                                <div class="card-body">
                                    <h5 class="card-title">Nouveau produit</h5>
                                    <p class="card-text text-muted">Nos dernières créations seront bientôt affichées ici.</p>
                                    <a href="{{ path('app_products') }}" class="btn btn-outline-primary">Voir la boutique</a>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% endif %}
            </div>
            <div class="row">
                <div class="col-12 text-center">
                    <a href="{{ path('app_products') }}" class="btn btn-outline-primary btn-lg" aria-label="Voir tous les produits">
                        <i class="fas fa-store me-2"></i>Voir tous nos produits
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- Why Choose Us -->
    <section class="py-5 bg-light section">
        <div class="container">
            <div class="row">
                <div class="col-12 text-center mb-5">
                    <h2 class="section-title">Pourquoi Nous Choisir ?</h2>
                    <p class="section-subtitle">L'excellence française à votre service</p>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-3 col-md-6 mb-4 text-center">
                    <div class="mb-3">
                        <i class="fas fa-flag text-primary" style="font-size: 3rem;"></i>
                    </div>
                    <h5>Made in France</h5>
                    <p class="text-muted">Tous nos produits sont fabriqués en France avec des matériaux de qualité.</p>
                </div>
                <div class="col-lg-3 col-md-6 mb-4 text-center">
                    <div class="mb-3">
                        <i class="fas fa-hands text-primary" style="font-size: 3rem;"></i>
                    </div>
                    <h5>Artisanat</h5>
                    <p class="text-muted">Un savoir-faire traditionnel transmis de génération en génération.</p>
                </div>
                <div class="col-lg-3 col-md-6 mb-4 text-center">
                    <div class="mb-3">
                        <i class="fas fa-shipping-fast text-primary" style="font-size: 3rem;"></i>
                    </div>
                    <h5>Livraison Rapide</h5>
                    <p class="text-muted">Livraison gratuite en France métropolitaine dès 50€ d'achat.</p>
                </div>
                <div class="col-lg-3 col-md-6 mb-4 text-center">
                    <div class="mb-3">
                        <i class="fas fa-heart text-primary" style="font-size: 3rem;"></i>
                    </div>
                    <h5>Service Client</h5>
                    <p class="text-muted">Une équipe dédiée pour vous accompagner dans vos choix.</p>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
